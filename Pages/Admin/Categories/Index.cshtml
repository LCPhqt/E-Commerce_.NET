@page
@model ECommerceFinalProject.Pages.Admin.Categories.IndexModel
@{
    ViewData["Title"] = "Quản lý danh mục";
    ViewData["ActivePage"] = "Categories";
}

<div class="page-header">
    <h4>Quản lý danh mục</h4>
    <a href="/Admin/Categories/Create" class="btn-admin primary"><i class="bi bi-plus-circle"></i> Thêm danh mục</a>
</div>

<div class="table-admin">
    <div class="table-responsive">
        <table class="table mb-0">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Tên danh mục</th>
                    <th>Mô tả</th>
                    <th>Số sản phẩm</th>
                    <th>Thao tác</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Categories.Any())
                {
                    <tr><td colspan="5" class="text-center py-4" style="color:var(--admin-text-muted);">Chưa có danh mục nào.</td></tr>
                }
                @foreach (var cat in Model.Categories)
                {
                    <tr>
                        <td>@cat.Id</td>
                        <td style="font-weight:500;color:var(--admin-text);">@cat.Ten</td>
                        <td style="color:var(--admin-text-muted);">@(cat.MoTa ?? "\u2014")</td>
                        <td>
                            <span class="badge-admin" style="background:rgba(99,102,241,0.08);color:#818cf8;">@(cat.Products?.Count ?? 0) SP</span>
                        </td>
                        <td>
                            <a href="/Admin/Categories/Edit?id=@cat.Id" class="btn-admin" style="padding:0.25rem 0.5rem;"><i class="bi bi-pencil"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
